rules:
  - name: reminder_helper_schedule_request
    pattern: "(.+?\\d{1,2}시(?:\\s*\\d{1,2}분)?(?:에)?\\s*.+?)\\s*(알람으로 보내줘|알림으로 보내줘|알람 보내줘|알람 해줘|리마인드해줘|리마인드 해줘)"
    tool: "reminder_helper"
    params:
      text: "{match_1} {match_2}"
