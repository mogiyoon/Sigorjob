# Communication Helper

- Use `communication_helper` for phone-call and SMS style requests.
- If a phone number is present, prefer `tel:` or `sms:` links.
- If only a recipient name and message body are present, return a local message draft instead of a generic search result.
